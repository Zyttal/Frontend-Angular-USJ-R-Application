export type Student = {
  studid: number,
  studfirstname: string,
  studmidname: string,
  studlastname: string,
  studcollid: number,
  studprogid: number,
  studyear: number
}

export type StudentRequest = {
  studID: number,
  studFirstName: string,
  studLastName: string,
  studMidName: string,
  studProgId: number,
  studCollId: number,
  studYear: number
}
